# Minimal habituation model (neurotape.habituation): the configuration the preregistered runs use.
# Every value not listed here is the HabConfig default; sources in ASSUMPTIONS.md § "Minimal habituation model".
# Frozen 2026-09-23 with docs/habituation/PREREG.md.
seed: 0
periphery: {kind: an}
