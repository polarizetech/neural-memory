# E01-stentor-map — Deviations

Schema: PREREG_PROTOCOL.md §5 (Willroth & Atherton 2024).

| # | Date | Stage/section | Type | Original text | Change | Reason | Outcome known? | Effect on interpretation | Commit |
|---|------|---------------|------|---------------|--------|--------|----------------|--------------------------|--------|

| 1 | 2026-09-24 | §3 SR3 criteria (analyse.py verdict) | criterion | "95 % t-CI upper bound < 0 for **both** inserts" → PASS, else FAIL | A minimum detectable effect (MDE) is added. MDE = t(0.975, 9) · SD / √10, SD = between-seed SD of the presentation-9/presentation-8 response ratio in EXISTING runs (1.5 s stimulus, ISI 3 s): `std` 0.0350 (hab_memory, SD 0.0490); `hebb_only` 0.0125 (sweep_eta_x0.25, SD 0.0174); `none` 0.0147. Rule: if \|mean effect\| < that arm's MDE the SR3 verdict is UNINTERPRETABLE, not FAIL (and not PASS); otherwise §3 applies unchanged. analyse.py stays frozen; RESULTS.md applies this rule on top of its verdict | operator, before any run: the analytic effect for `std` (−0.21 %, −0.33 %) is ~10× below the detectable size, so a FAIL would report an undetectable effect as a negative | no | a small effect can no longer be scored as a failed prediction. Caveats: the SD is a proxy (1.5 s not 0.2 s stimulus; consecutive presentations, not paired series with shared noise), so the MDE is conservative for the paired SR3 design. Predicted SR3-`std` verdict under this rule: UNINTERPRETABLE | (this commit; tag E01-stentor-map-interim-1) |
| 2 | 2026-09-24 | EXPERIMENT_PR_LOG.md §1 (log location) / CONVERSATIONS.md | environment | the experiment's agent conversation is logged to `experiments/E01-stentor-map/conversation.jsonl` and mirrored to the PR | the SETUP session (move to this repo, kit install, model-v0.1.0, prereg, interim-1) was NOT captured by convo-log: it was started from the audio-projects monorepo folder, so this repo's hooks never fired, and no conversation.jsonl exists for it. The run turns (step 4) will be started from this repo so they are logged | the session predates the kit install and ran from another working directory | no | the record of how the prereg was written is the commit history, tags and the chat summaries, not a verbatim log; nothing about the run itself is affected | (this commit) |
| 3 | 2026-09-24 | §10 Timestamp / EXPERIMENT_PR_LOG.md §1 receipts | environment | a receipt is posted to the PR when each tag is made | the receipts for model-v0.1.0 (tagged 10:32:08 −07:00), E01-stentor-map-prereg (10:32:24) and E01-stentor-map-interim-1 (12:42:23) were all posted at 12:54 −07:00 (19:54Z), after the repo and PR existed (PR #1, comments 5821194398 / 5821194736 / 5821195056). They hash PREREG.md and ENV.lock AS COMMITTED AT THE TAG (0a4fa762… / d3f81994…, identical to the values computed by hand at tagging) | no GitHub remote or gh at tagging time; and the kit's prereg-receipt read the EID wrongly (fixed in the kit, e5f1294) and could not find gh off PATH (1080b8e) | no | the third-party clock bounds all three tags only from above, at 12:54 −07:00: it shows the prereg and interim plan existed by then, not that they predate 12:54. No SR run had started by then, so the ordering prereg → interim → run is still evidenced by the PR | (this commit) |
| 4 | 2026-09-24 | §3 SR3 + DEVIATIONS #1 (how the MDE rule combines the two inserts) | analysis | #1: "if \|mean effect\| < that arm's MDE the SR3 verdict is UNINTERPRETABLE ... otherwise §3 applies unchanged"; §3: PASS needs BOTH inserts to deepen | The MDE rule is applied PER INSERT, and the arm's verdict combines them: FAIL if any insert is at or above its MDE and not deepening; PASS if both deepen beyond the MDE; otherwise UNINTERPRETABLE. analyse.py's frozen per-arm FAIL is reported beside it | #1 does not say how to combine two inserts, and the arms split: `std` has both inserts under its MDE; `hebb_only` has +10 dB under (−0.74 % vs 1.25 %) and +20 dB beyond it and deepening (−3.46 %, CI [−5.92 %, −1.00 %]) | **yes** — decided after seeing SR3's output | this cannot turn a FAIL into a PASS: under it no SR3 arm passes. It changes `hebb_only` from FAIL (analyse.py, because +10 dB's CI crosses 0) to UNINTERPRETABLE; the stricter reading (FAIL) is given in RESULTS.md beside it | (this commit) |
